// Web has no react-native-maps. tracking.tsx already renders the map
// conditionally and falls back to the "Open in Maps" link, so null is the
// whole shim.
export const MapView = null;
export const Marker = null;
